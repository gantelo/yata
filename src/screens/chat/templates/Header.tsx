import { MidHeader } from '@components';

import { HeaderContent, Search } from '../molecules';

const ChatHeader = () => {
	return (
		<MidHeader>
			<HeaderContent />
			<Search />
		</MidHeader>
	);
};

export default ChatHeader;
